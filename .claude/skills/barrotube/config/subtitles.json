{
  "_comment": "자막 렌더 모드. mode=static(기존 문구 PNG 오버레이) | karaoke(워드 단위 색 스윕, TTS 싱크). 해석 우선순위: env BT_SUBTITLE_MODE > channels[channel_id] > default. 파일이 없거나 채널 항목이 없으면 static(무변화). 새 채널을 켜려면 channels 에 항목 추가.",
  "default": { "mode": "static" },
  "channels": {
    "econ-daily": {
      "mode": "karaoke",
      "base_color": "#FFFFFF",
      "highlight_color": "#FF9A1F",
      "outline_color": "#081320",
      "font_size": 60
    }
  }
}
